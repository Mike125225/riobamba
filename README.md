# Riobamba SLT 2027 landing page

Live at https://mike125225.github.io/riobamba/ (GitHub Pages, `main` branch).

Source of truth also mirrored in `AIS-OS/projects/medlife/slt-riobamba-landing/`.
After editing `index.html`, run: `git add -A && git commit -m "..." && git push`.
